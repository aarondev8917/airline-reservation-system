# ============================================================================
# TEST CONFIGURATION (base) - always active when running tests
# ============================================================================
# Ensures 'test' profile is used so application-test.properties is loaded.
# application-local.properties is NOT imported (dev-only). Aviationstack
# uses mock data in tests; no config toggling needed when switching app vs tests.
spring.profiles.active=test
